# absolute path to the directory that contains the synced timeline inputs
TIMELINE_DATA_DIRECTORY=/path/to/timeline-data

# wait for Syncthing writes to settle before processing
TIMELINE_SETTLE_SECONDS=30
TIMELINE_SETTLE_ATTEMPTS=10
